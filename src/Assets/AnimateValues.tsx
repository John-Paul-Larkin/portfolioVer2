const animate = {
  opacity: 1,
  y: 0,
  x: 0,
  transition: {
    ease: "easeOut",
    duration: 0.3,
  },
};

const initialLeft = {
  opacity: 0.1,
  x: -20,
};

const initial = {
  opacity: 0.1,
  y: 20,
};

const initialRight = {
  opacity: 0.1,
  x: 20,
};

export { animate, initial, initialLeft, initialRight };
